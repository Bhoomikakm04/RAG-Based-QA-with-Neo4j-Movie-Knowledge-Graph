# Security Checklist

- Rotate any Neo4j password that has been exposed in screenshots or shared files.
- Keep credentials only in `.env`.
- Add `.env` to `.gitignore`.
- Use read-only Cypher for user-driven retrieval.
- Do not execute arbitrary Cypher generated by an untrusted model.
- Do not publish Aura connection credentials in the report or GitHub.
